{
  "name": "nangraph.cpp",
  "version": "1.0.0",
  "description": "A pure C++ library that represents a graph data structure",
  "scripts": {
    "configure": "ngyp gyp/package.gyp --depth=. -f make --generator-output=build --toplevel-dir=.",
    "clean": "rm -rf build",
    "test": "make --directory=build && build/out/Default/test "
  },
  "keywords": [
    "c++",
    "graph"
  ],
  "author": "Andrei Kashcha",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anvaka/nangraph.cpp"
  },
  "devDependencies": {
    "catch.cc": "6.0.0",
    "ngyp": "^1.0.1"
  }
}
